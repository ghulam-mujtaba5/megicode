# ==============================
# Auth (Internal Portal)
# ==============================

# Generate a strong secret (example): `openssl rand -base64 32`
AUTH_SECRET=

# Google OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Optional: restrict who can sign in to /internal
# If both are empty, any Google account can sign in (NOT recommended for production)
INTERNAL_ALLOWED_EMAILS=
INTERNAL_ALLOWED_DOMAINS=

# Grant admin role automatically to these emails (comma-separated)
INTERNAL_ADMIN_EMAILS=

# Default role for new users (viewer|pm|dev|qa)
INTERNAL_DEFAULT_ROLE=viewer

# Enable quick dev login (email-only, no password) - useful for testing
DEV_LOGIN_ENABLED=true


# ==============================
# Database (Turso / libSQL)
# ==============================

TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=


# ==============================
# Email (Zoho SMTP + Resend)
# ==============================

# Provider selection: auto | zoho | resend
EMAIL_PROVIDER=auto

# Shared sender address used by Resend (must be verified) and as a fallback
EMAIL_FROM=

# Existing names kept for compatibility
FROM_EMAIL=
TO_EMAIL=

# Contact form routing
CONTACT_TO_EMAIL=
CONTACT_FROM_EMAIL=

# Resend API Key
# Get your key from https://resend.com/api-keys
RESEND_API_KEY=

# Zoho SMTP (already used in the project)
ZOHO_USER=
ZOHO_PASS=
